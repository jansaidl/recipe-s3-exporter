# Build artifacts
/app
/bin/tailwindcss
*.out

# Note: internal/web/static/css/app.css is generated by Tailwind but committed
# so a fresh clone runs without the Tailwind CLI; the Zerops build regenerates it.

# Local env
.env

# OS / editor
.DS_Store
.idea/
